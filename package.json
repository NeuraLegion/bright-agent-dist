{
  "name": "bright-agent-dist-tools",
  "version": "1.0.0",
  "private": true,
  "description": "STAR Workflow Builder — static GitHub Pages app and its unit tests.",
  "scripts": {
    "test": "node --test"
  }
}
